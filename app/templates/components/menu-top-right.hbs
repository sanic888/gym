<div class="{{menuClassNames}}">
	<div {{action 'toggleMenu'}}>
		Alexander Yancharuk &nbsp;
		{{#if expanded}}
			<i class="glyphicon glyphicon-triangle-top"></i>
		{{else}}
			<i class="glyphicon glyphicon-triangle-bottom"></i>
		{{/if}}
	</div>
	<div {{action 'goTo' 'profile'}}>Profile</div>
	<div {{action 'logout'}}>Log Out</div>
</div>