<div class="header">
	{{#if session.isAuthenticated}}
		<button {{action 'logout'}}>Logout</button>
	{{else}}
		{{#link-to 'login'}}
			Login
		{{/link-to}}
	{{/if}}
</div>

<div class={{menuClasses}}>
	{{menu-left goTo='changeState' hideMenu='hideMenu' showMenu='showMenu' currentRoute=currentRoute}}
</div>

<div class="content">
	<div class="content-wrapper">
		{{outlet}}		
	</div>
</div>
